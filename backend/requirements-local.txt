# Local voice pipeline — only needed when LOCAL_VOICE=true (your laptop)
# Install with: pip install -r requirements.txt -r requirements-local.txt
-r requirements.txt

openwakeword==0.6.0    # wake word engine (hey_nova or hey_jarvis fallback)

# Wake word training deps (only needed to run train_hey_nova.py)
torchinfo
torchmetrics
tqdm
pyyaml
pronouncing
audiomentations
torch-audiomentations
speechbrain
torchaudio
mutagen
acoustics
onnxscript
faster-whisper==1.0.3  # local STT fallback
groq                   # cloud STT — Whisper large-v3
edge-tts==6.1.12       # Greek TTS
gTTS                   # Google TTS fallback
miniaudio              # mp3 decode → PCM
sounddevice==0.4.7     # audio record + playback
numpy==1.26.4
scipy==1.13.1
resemblyzer==0.1.4     # speaker verification
